#include <stdio.h>

typedef struct {
    double x[3];
} test;

int main() {
    test t = {{1, 2, 3}};
    test t2 = t;
    printf("t1.x = %p: %lf, %lf, %lf\n", t.x, t.x[0], t.x[1], t.x[2]);
    printf("t2.x = %p: %lf, %lf, %lf\n", t2.x, t2.x[0], t2.x[1], t2.x[2]);
    return 0;
}
